[deploy]
startCommand = "/app/.venv/bin/python manage.py migrate --noinput && /app/.venv/bin/python manage.py collectstatic --noinput && /app/.venv/bin/gunicorn config.wsgi --bind 0.0.0.0:$PORT --log-file - --access-logfile -"
healthcheckTimeout = 300

